# AI 实体按键交互 PRD

## 1. 产品目标

学生可在任意页面通过 AI 实体键发起语音或拍照提问；AI 对话框已打开时，通过实体键继续当前对话。

## 2. 按键定义

- 单击：发起语音输入。
- 双击：发起拍照输入。

## 3. 会话逻辑

| 当前状态 | 按键操作 | 产品逻辑 |
|---|---|---|
| AI 对话框未打开 | 单击 | 新建 AI 会话并开始收音；语音发送后打开 AI 对话框 |
| AI 对话框未打开 | 双击 | 新建 AI 会话并打开相机；确认后打开对话框，图片进入输入区待发送 |
| AI 对话框已打开 | 单击 | 继续当前会话，效果等同于点击对话框内的语音输入按钮，复用原有语音输入交互 |
| AI 对话框已打开 | 双击 | 继续当前会话并打开相机；确认后返回对话框，图片进入输入区待发送 |

- 通过实体键新建的 AI 会话不展示欢迎语，用户发送内容后 AI 再回答。
- “新建会话”指不续接上一次会话上下文，不删除历史会话记录。
- “继续当前会话”时保留已有消息及上下文。

## 4. 单击逻辑

### 4.1 AI 对话框未打开

1. 单击后立即开始收音。
2. 页面底部展示轻量收音模块。
3. 用户结束并发送语音后，打开 AI 对话框并进入新会话。
4. 语音发送、消息展示、AI 回答及异常处理复用原有 AI 对话逻辑。

### 4.2 AI 对话框已打开

1. 单击效果等同于点击对话框内的语音输入按钮。
2. 语音录制、取消、发送及消息展示全部复用原有交互。
3. 录音过程中再次单击 AI 键，结束录音并发送。
4. AI 正在回答时单击，停止当前回答并进入语音输入状态，已有对话内容保留。

## 5. 双击逻辑

1. 双击后直接打开相机。
2. 支持连续拍摄，单次最多选择 9 张照片。
3. 确认照片后打开新会话或返回当前会话，图片进入输入区待发送。
4. 确认照片不触发发送或 AI 回答；学生可继续补充文字或语音，由学生主动触发发送。
5. 拍照、重拍、删除、取消、发送及消息展示复用原有图片输入交互。
6. 录音过程中双击，取消尚未发送的录音并打开相机。

## 6. 复用与限制

- AI 实体键与原有 AI 入口共用权限、任务限制和可用状态，不得绕过原有管控。
- AI 不可用时，不启动录音、相机或内容上传，提示文案复用原有不可用提示。
- 会话创建、上下文传递、内容审核、发送状态、失败重试及异常提示均复用原有 AI 对话逻辑。
- 语音录制、相机和页面底部收音模块不可同时处于激活状态。
