# AI 实体按键交互 PRD

## 1. 产品目标

学生可在任意页面通过 AI 实体键发起语音或拍照提问；AI 对话框已打开时，通过实体键继续当前对话。<br><font color="#17823b">其中，“AI 对话框已打开”仅指 AI 实体键唤起的抽屉已打开；首页 AI 助教对话页及各功能内的 AI 对话页均属于底层应用页面，从这些页面按键时仍新建对话并在上层弹出抽屉。</font>

## 2. 入口归属

- AI 实体键统一使用首页“通用 AI 助教”，不唤起当前页面的场景 AI。
- 当前页面存在“问问龙老师”等场景 AI 入口时，实体键仍进入通用 AI 助教。
- 实体键打开的对话框与首页 AI 助教保持一致，复用原有快捷功能、对话能力及交互。
- 实体键产生的历史对话统一保存在首页 AI 助教的历史对话中。
- 使用时长、权限及管控数据统一归入首页 AI 助教，不单独增加实体键业务类型。

## 3. 按键定义

- 单击：发起语音输入。
- 双击：发起拍照输入。

## 4. 会话与按键响应

| 当前状态 | 单击 | 双击 |
|---|---|---|
| AI 对话框未打开 | 新建 AI 会话并开始收音；语音发送后打开 AI 对话框 | 新建 AI 会话并打开相机；确认照片后打开 AI 对话框，图片进入输入区待发送 |
| AI 对话框已打开 | 继续当前会话，效果等同于点击对话框内的语音输入按钮 | 继续当前会话，效果等同于点击对话框内的拍照按钮 |

- 每次从未打开状态唤起 AI 对话框，均新建会话，不续接上一次会话上下文。
- 新建会话不展示 AI 欢迎语，用户发送内容后 AI 再回答。
- AI 对话框已打开时，保留当前会话的消息和上下文。
- <font color="#17823b">底层应用内的 AI 对话页不视为 AI 实体键抽屉；从此类页面按键时，仍新建 AI 实体键会话并在上层弹出抽屉。</font>
- 新建会话不删除已有历史对话。

## 5. 输入交互

### 5.1 语音输入

- AI 对话框未打开时，单击后立即开始收音，页面底部展示轻量收音模块。<br><font color="#17823b">收音模块包含麦克风、波形、计时、取消和发送，并提示“正在录音”。</font>
- 用户结束并发送语音后，打开 AI 对话框并展示本次新会话。<br><font color="#17823b">点击取消：丢弃本次录音并收起模块；点击发送：结束录音并打开新会话。</font>
- AI 对话框内的语音录制、取消、发送及消息展示复用原有语音输入交互。<br><font color="#17823b">AI 实体键抽屉已打开时，单击等同点击抽屉内的语音输入按钮，不展示 Toast。</font>
- <del>录音过程中再次单击实体键，结束录音并发送。</del><br><font color="#17823b">录音过程中再次单击实体键，丢弃本段录音并重新开始计时，提示“正在重新录音”，不触发发送。</font>
- AI 正在回答时单击实体键，停止当前回答并进入语音输入状态，已有消息保留。

### 5.2 拍照输入

- 双击后直接打开相机。
- <del>支持连续拍摄，单次最多选择 9 张照片。</del><br><font color="#17823b">每次只拍摄 1 张照片，不支持连拍。</font>
- <font color="#17823b">按下拍摄按钮后进入照片预览，可选择重拍或确认。</font>
- 确认照片后，图片进入对话输入区，不自动发送，也不触发 AI 回答。
- 学生可继续补充文字或语音，由学生主动触发发送。
- 拍照、重拍、删除、取消、发送及消息展示复用原有图片输入交互。
- 录音过程中双击实体键，取消尚未发送的录音并打开相机。

### 5.3 未发送内容处理

- 输入区存在未发送文字时单击实体键：清空未发送文字并进入语音输入状态。
- 输入区同时存在图片时，只清空文字，待发送图片保留。
- 输入区存在待发送图片时双击实体键：保留已有图片并继续拍照，图片总数不超过 9 张。
- 图片与后续补充的文字或语音作为同一条消息发送。

## 6. 展示层级与页面状态

- AI 对话框采用半屏形式，展示在当前应用页面上层。
- 打开 AI 对话框时，底层页面保持原有状态。
- <font color="#17823b">AI 实体键唤起的对话弹窗覆盖下方应用页或应用页内 AI 对话时，下层不计时，仅记录最上层页面或弹窗的使用时长。</font>
- <font color="#17823b">AI 对话弹窗、音量弹窗和电源选项弹窗按照触发先后确定展示层级，后触发的弹窗展示在上层。</font>
- AI 对话框不因底层页面内部的普通切换或刷新自动关闭。
- AI 对话触发跨页面跳转时，关闭当前对话框后执行跳转。
- <font color="#17823b">AI 对话抽屉内触发功能跳转时，先关闭对话抽屉，再处理底层页面的离开流程；底层页面需要保存或弹出二次确认时，须先完成保存或确认，再跳转至目标页。从目标页返回后，重新打开原 AI 对话抽屉并恢复原会话，便于用户继续执行下一步操作。</font>

## 7. 系统状态与资源占用

| 系统或设备状态 | 按键处理 |
|---|---|
| 关机 | 不响应 |
| 锁屏 | 不响应 |
| <font color="#17823b">老师一键锁屏后</font> | <font color="#17823b">不响应</font> |
| <font color="#17823b">设置页</font> | <font color="#17823b">不启动 AI，Toast 提示“设置页无法使用AI按键”</font> |
| 未登录 | 不启动 AI，Toast 提示“需登录后使用” |
| 麦克风权限关闭 | 复用原有麦克风权限提示及开启流程 |
| 相机权限关闭 | 复用原有相机权限提示及开启流程 |

- AI 实体键与首页 AI 助教共用权限、任务限制和可用状态，不得绕过原有管控。
- AI 不可用时，不启动录音、相机或内容上传，复用原有不可用提示。<br><font color="#17823b">AI 被禁用、不在可用时段或使用时长超出限制均属于 AI 不可用状态，弹出对应的原有禁用提示。</font>
- 会话创建、内容审核、发送状态、失败重试及异常提示复用原有 AI 对话逻辑。
- 调起 AI 语音或相机时，停止当前页面正在进行的音频播放、音频录制、视频播放、视频录制或拍照。
- 语音录制、相机及页面媒体能力不可同时占用麦克风或相机。
